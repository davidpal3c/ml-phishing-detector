chrome.runtime.onInstalled.addListener(() => {
    console.log("Phishing Detector Extension installed.");
});
  
// Context menu / page scrape hooks
// Content script messaging
// Email scraping from Gmail (if permitted)